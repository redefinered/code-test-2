import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  root: {
    padding: 15,
  },
  image: {
    marginBottom: 10,
  },
  track: {
    fontSize: 20,
    fontWeight: 'bold',
    marginBottom: 5,
  },
});
